# 오시요 (Osiyo)

집회 신고 정보를 모아 두 종류의 사용자에게 서로 다른 답을 주는 웹앱.

- **참여하려는 사람**: 집회 검색, 준수사항 안내, 익명 참여 예정 표시
- **피해가려는 사람**: 지도, 해산 시각, 혼잡도, 통제 우회 길찾기

프로토타입(`reference/오시요_프로토타입.html`)은 화면 흐름과 데이터 구조를 파악하기 위한
참고 자료다. 최종 디자인·인터랙션·정보구조는 그보다 더 나은 수준으로 만든다.

## 절대 규칙 (변경 불가)

1. **개인 식별자를 저장하지 않는다.** 로그인·회원가입·계정 시스템을 만들지 않는다.
2. **참여 예정 표시는 집계 숫자만 남긴다.** 누가 눌렀는지 저장하는 코드(세션, 쿠키 식별자,
   IP 로깅 등)를 작성하지 않는다. `POST /api/assembly/[id]/attend` 같은 엔드포인트는
   요청 본문·헤더에서 어떤 식별자도 읽거나 남기지 않고 카운터만 증가시킨다.
3. **맞불 집회는 구역 단위 위험 신호로만 표시한다.** 특정 집단의 실시간 위치를 다른 집단에게
   노출하는 기능(정확한 좌표, 인원 이동 방향 등)을 만들지 않는다. 항상 지역/구역 단위로 뭉뚱그린다.
4. **자체 내비게이션 엔진을 만들지 않는다.** 통제 구간을 제외한 경유지를 계산해
   카카오맵/네이버지도/티맵 딥링크로 넘긴다. 턴바이턴 실시간 안내는 외부 지도 앱의 역할이다.
5. **모든 집회 정보에는 출처(경찰청/서울시)와 기준 시각을 함께 표시한다.** 출처·시각 없이
   집회/혼잡도/통제 데이터를 화면에 노출하지 않는다.
6. **정확성 면책 문구를 유지한다.** 예보·혼잡도·통제 예상시간은 추정치이며 실제와 다를 수
   있음을 관련 화면에 명시한다.

이 규칙은 기능 요청보다 우선한다. 규칙과 충돌하는 요청을 받으면 구현 전에 문제를 제기한다.

## 기술 스택

- Next.js 16 (App Router, Turbopack) + TypeScript + Tailwind CSS v4
- 데이터: 초기에는 JSON 파일 기반 → 이후 SQLite(Prisma)로 전환
- 지도: Kakao Maps JavaScript SDK
- 배포 대상: Vercel
- 패키지 매니저: pnpm

> Next.js 16은 15와 관례가 다를 수 있다. 확신 없는 API는 추측하지 말고 실제 동작을
> 브라우저/터미널에서 확인한 뒤 코드를 작성한다.

## 폴더 구조

```
src/app/          라우트 (App Router)
src/components/   재사용 UI 컴포넌트
src/lib/          데이터 접근, 계산 로직, 외부 API 클라이언트
src/types/        타입 정의
data/             목업/샘플 JSON 데이터
reference/        프로토타입 등 참고 자료 (빌드에 포함되지 않음)
```

## 디자인 원칙

프로토타입의 톤(신뢰감 있는 navy + 경고/강조용 amber)은 유지하되, 다음을 프로토타입보다
개선한다:

- **타이포그래피**: Pretendard Variable을 로컬에서 서빙(`next/font/local`)해 FOUC 없이
  로드한다. 한국어 줄바꿈은 `word-break: keep-all` 기본 적용.
- **디자인 토큰화**: 색상·간격·radius를 Tailwind `theme`에 등록해 임의값(`style="..."`)
  남발을 피한다. `navy #13234A`, `amber #F2A93B`, 배경 `#F5F7FB`를 기준으로 확장된
  팔레트(hover/pressed/disabled 상태 포함)를 정의한다.
- **접근성**: 색만으로 상태를 구분하지 않는다(아이콘·텍스트 병행). 명도 대비 WCAG AA 이상.
  `prefers-reduced-motion` 존중.
- **상태 처리**: 로딩(skeleton)·빈 상태·에러 상태를 모든 데이터 화면에 설계한다.
  외부 API(서울 열린데이터광장 등) 실패 시 "데이터 없음"을 명확히 보여주고 앱이 깨지지
  않게 한다.
- **출처 배지**: "출처 + 기준 시각"을 하나의 재사용 컴포넌트(`SourceTag` 등)로 만들어
  모든 데이터 카드에 일관되게 부착한다.
- **모바일 우선**: 최대 폭 480px 중앙 정렬, 하단 탭바(홈/지도/예보/내정보).
  데스크톱에서도 어색하지 않도록 넓은 화면 레이아웃을 별도로 고려한다.

## 작업 방식

- 파일이 여러 개 생기는 작업 전에는 먼저 계획을 보여주고 승인받는다.
- 화면을 만들면 브라우저에서 실제로 확인 가능한 상태로 만든다.
- 스크래핑(6단계, 서울경찰청 게시판)은 반드시 실제 HTML 구조를 먼저 확인한 뒤 시작한다:
  robots.txt 확인, User-Agent 명시, 요청 간 최소 2초 간격, 실패 시 재시도 3회까지.
